# Storage Valet v3.1 — Portal Environment Variables
# All values below are PUBLIC and safe to expose client-side

# Supabase (public keys only)
VITE_SUPABASE_URL=https://REPLACE_ME_SUPABASE_URL.supabase.co
VITE_SUPABASE_ANON_KEY=REPLACE_ME_ANON_KEY

# App
VITE_APP_URL=http://localhost:5173

# Stripe (publishable key only - TEST mode for staging)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME_STRIPE_PUBLISHABLE_KEY

# IMPORTANT: Never add service_role keys, Stripe secret keys, or webhook secrets here.
# Those belong in Supabase Edge Functions environment (set via `supabase secrets set`).
